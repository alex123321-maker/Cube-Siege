# Объединение наработок в main — 2026-09-25

Проверены все 14 исходных локальных веток Cube-Siege, ветки `origin` после `git fetch --prune`, список worktree и stash. Для этого репозитория зарегистрирован один рабочий каталог: `D:/Repository/game`. До проверки stash был пуст. Отдельный соседний репозиторий `assets.git` не входит в этот аудит.

## Решения по веткам

| Ветка | Результат |
| --- | --- |
| `feat/26-terrain-material-kit` | Объединены три коммита: материалы ландшафта, документация и инструменты сравнения. |
| `feat/20-vertical-slice-audit` | Перенесён оставшийся коммит `90f82b2`: ссылка в STATUS и диапазон этапов дорожной карты. |
| `feat/6-production-character-models-vfx` | Повторный merge не нужен: дерево вершины `6ad7d9d` совпадает с уже включённым squash-коммитом `ed8f905` (#7). |
| Остальные исходные ветки | Их вершины уже являются предками актуального `origin/main` (`bc98904` на начало аудита). |

## Незакоммиченные наработки

- Сохранены Blender-исходник воина, runtime GLB, текстуры, контракты модели и регрессионные тесты.
- Сохранён отдельный Blockbench-вариант в `art/characters/warrior_reference`; он остаётся художественным исходником для сравнения и не подменяет игровую модель.
- Включены инструменты подготовки/проверки ассетов, калибровочный ассет и документация.
- Включены общий процедурный слой анимации, профили трёх классов, циклы шага, тесты, стенды и прежние записи проверок.
- Восстановлены недостающие подключения профиля класса, physics delta, ориентации и F3/F4 в контроллере игрока. Исправлено неверное свойство `Label3D.outline_color`, вызывавшее исключение при F3.
- Исправлен инструмент съёмки ландшафта: seed устанавливается до генерации, чанки загружаются для выбранной точки, камера учитывает высоту земли, снимок сохраняется после рендеринга. Семь runtime-снимков пересняты.
- Документальные изображения исключены из импорта Godot через `.gdignore`, включая старые повреждённые concept WebP, не используемые игрой.

Старый временный `review_comment.txt` и неиспользуемая обёртка `.agents/tools/review_loop/register.py` сохранены локально в игнорируемой папке `art/work/consolidation-preserved/`. Кэши, локальные окружения и промежуточные файлы не включены. Исходное состояние всех изменений дополнительно сохранено в stash с сообщением `Safety snapshot before main consolidation 2026-09-25`; stash не удалён.

## Проверка

- Полный `python tools/verify.py`: PASS — инструменты, submodule, сборка C++, импорт Godot, GUT, Python-тесты, проверки процедурного мира/боя #18 и 100 кадров запуска.
- Новые проверки подключения сначала воспроизвели три отказа, после исправлений файл `test_character_presentation.gd` прошёл **12/12 тестов, 223 assertions**.
- Отдельный запуск настоящей `scenes/main.tscn`, 100 кадров: код выхода 0, без runtime-ошибок.
- Три графических запуска стенда анимации: воин, лучник, инженер; по 440 physics ticks, код выхода 0, пустой stderr. Кадры просмотрены.
- Графический запуск инструмента ландшафта: семь снимков, код выхода 0, пустой stderr. Просмотрены лес и горы.
- Python-файлы проверены синтаксическим разбором; `git diff --check` выполнен перед фиксацией.

Игровые сохранения изолированы через отдельный APPDATA; пользовательский прогресс не сбрасывался. Сохраняются прежние предупреждения Godot Editor о ресурсах/ObjectDB при завершении. Это не полная игровая или художественная приёмка; прежние числа FPS и видеозаписи в документах относятся к их исходным прогонам. Полный журнал текущего verify и регрессионного прогона: [`verification/consolidation-2026-09-25/`](verification/consolidation-2026-09-25/).
